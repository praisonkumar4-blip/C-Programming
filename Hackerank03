#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	
    int n,m,o;
    float a,b,c,d;
    
    scanf("%d %d",&n,&m);
    scanf("\n%f %f",&a,&b);
    printf("%d %d",n+m,n-m);
    c=a+b;
    d=a-b;
    printf("\n%.1f %.1f",c,d);
    return 0;
}
